version: 2.1

jobs:
  say-hello:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: echo Hello World! From
  say-world:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: echo $MY_NAME
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - say-world:
          requires:
            - say-hello
